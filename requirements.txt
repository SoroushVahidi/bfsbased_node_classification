# Requirements

# ── Core numeric / graph stack ──────────────────────────────────────────────
# Install PyTorch (CPU-only is sufficient for all experiments in this repo):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then install the remaining packages:
#   pip install -r requirements.txt

torch>=2.0.0
torch_geometric>=2.3.0
numpy>=1.23.0
networkx>=2.8
sortedcontainers>=2.4.0
matplotlib>=3.6.0

# ── Optional (only for HPC / Slurm job submission) ──────────────────────────
# No additional Python packages required.

# ── Table/figure regeneration only (no PyTorch needed) ───────────────────────
# numpy and matplotlib (already listed above) are sufficient for:
#   bash scripts/run_all_prl_results.sh
