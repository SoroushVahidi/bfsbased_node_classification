This folder holds the **frozen 10-split per-split export** (`final_tables_prl.csv`) used to rebuild `reports/final_method_v3_results.csv` (see `scripts/prl_final_additions/rebuild_final_v3_results_10split.py`).

- **Do cite** merged means ± std from **`tables/main_results_prl.md`** (or regenerate via `bash scripts/run_all_prl_results.sh`).
- **`final_tables_prl.md`** is a snapshot of pre-merged tables; prefer the regenerated `tables/` files for the manuscript so numbers stay aligned with the canonical CSV.

The directory name predates standardization; the CSV here is **not** superseded — it is part of the reproducibility trail for the 10-split package.
