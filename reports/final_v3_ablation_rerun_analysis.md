# FINAL_V3 ablation rerun analysis

- This rerun executes all ablation rows on the current merged `FINAL_V3` code path over 6 datasets × 10 splits.
- Existing `tables/ablation_selective_correction.*` remains a mixed artifact (recomputed FINAL_V3 + historical rows), so this rerun is cleaner for manuscript evidence.
- Historical/mixed status detected in previous table: yes.
- Recommendation: keep the old table as archival provenance and cite `tables/ablation_selective_correction_rerun.*` as the fresh canonical-candidate ablation evidence.
